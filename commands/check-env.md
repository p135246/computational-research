Run `${CLAUDE_PLUGIN_ROOT}/scripts/check-env.sh` using the Bash tool and display the full output.

After showing the output, summarize:
1. Whether the Wolfram kernel is available and working
2. Whether the Wolfram MCP server is detected
3. What to do if anything is missing (include the exact install URL if relevant)

If both are available, confirm the environment is ready for Wolfram research.
