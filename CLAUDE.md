# Plugin Maintenance

After any changes to plugin files, always:
1. Commit and push to git
2. If the version was bumped, also update the version in `../claude-plugins/.claude-plugin/marketplace.json`, commit and push that repo too
